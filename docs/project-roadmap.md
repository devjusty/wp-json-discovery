# Project Roadmap

- Stabilize full-suite frontend execution: `AdminPage.test.tsx` intermittently fails under the complete Vitest run while passing in isolation; investigate shared test state before relying on the full frontend gate.
